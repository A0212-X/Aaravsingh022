# Google Gemini key used by the AI consultation, lab explanation, and visit prep features.
# Keep this file as a template only. Never commit a real key.
GEMINI_API_KEY="your-gemini-api-key"

# Optional port for local development or AWS App Runner.
PORT=3001
